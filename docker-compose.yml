version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=change-me-to-something-secret
      - FLASK_ENV=production
      - DATABASE_URL=sqlite:///ems_database.db
    volumes:
      - .:/app
    restart: always
